# Nexus dev/test requirements.
#
# Pinned to a single known-good pytest minor line. The pin encodes
# what has actually been exercised in the mesh-register probe pytest
# matrix (Python 3.10 / 3.11 / 3.13) — bump both the upper bound and
# the comment below in lockstep when you re-test on a newer 8.x.
#
# Local sanity: `python3 -m pytest --version` reports pytest-8.4.x.

pytest>=8.4,<8.5

# Coverage for SonarQube (plan 8261643): pytest-cov produces lcov reports
# consumed via sonar.python.coverage.reportPaths. Pinned to the pytest-cov
# line compatible with pytest 8.4 (pytest-cov 5.x/6.x both support it;
# pin 6.x as the known-good current line).
pytest-cov>=6.0,<7.0