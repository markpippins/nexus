/**
 * jev-inquiry — TypeSpec Main
 *
 * Entry point for the Jev bounded judgment contract.
 * Re-exports models from models.tsp.
 */
import "./models.tsp";

namespace org.nexus.jev.inquiry {
  // Models are exported from models.tsp
}