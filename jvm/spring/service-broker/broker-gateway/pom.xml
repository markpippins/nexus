<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>com.aibizarchitect.nexus</groupId>
        <artifactId>nexus</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>broker-gateway</artifactId>
    <packaging>jar</packaging>

    <name>Broker Gateway</name>
    <description>Nexus Broker Gateway</description>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>2025.0.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>broker-service-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>broker-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>broker-service-spi</artifactId>
        </dependency>
        <!-- user-service removed - deprecated social media functionality -->
        <!-- User authentication now handled by user-access-service (PostgreSQL) + login-service (Redis) -->

        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>search-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>broker-discovery-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>file-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>user-creation-service</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
        <!-- <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>export-service</artifactId>
        </dependency>        
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>upload-service</artifactId>
        </dependency> -->
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>login-service</artifactId>
        </dependency>
        <dependency>
            <groupId>com.aibizarchitect.nexus</groupId>
            <artifactId>nexus-user-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-mongodb</artifactId>
        </dependency>
        <!-- JPA and H2 Database for user-access-service (MySQL-based auth) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- PostgreSQL Connector -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Test dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                        <!-- No classifier: the main artifact IS the boot jar (atlas-consistent),
                             so `java -jar target/broker-gateway-*.jar` works without a -exec suffix. -->
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>