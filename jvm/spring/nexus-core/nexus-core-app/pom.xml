<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.nexus.core</groupId>
        <artifactId>nexus-core</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>nexus-core-app</artifactId>
    <name>nexus-core-app</name>
    <description>Runnable bootstrap — assembles the nexus-core read-only monolith on one port (:8090)</description>

    <dependencies>
        <dependency>
            <groupId>org.nexus.core</groupId>
            <artifactId>nexus-core-search</artifactId>
        </dependency>
        <dependency>
            <groupId>org.nexus.core</groupId>
            <artifactId>nexus-core-broker</artifactId>
        </dependency>
        <dependency>
            <groupId>org.nexus.core</groupId>
            <artifactId>nexus-core-solscript</artifactId>
        </dependency>
        <dependency>
            <groupId>org.nexus.core</groupId>
            <artifactId>nexus-core-writequeue</artifactId>
        </dependency>
        <dependency>
            <groupId>org.nexus.core</groupId>
            <artifactId>nexus-core-meep</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>